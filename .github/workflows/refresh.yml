name: Refresh notices

# Turns the Google Alerts RSS feed into public/notices.json every 6 hours.
# The commit triggers the deploy workflow, which rebuilds with fresh data.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: "3.12"
      - name: Build public/notices.json
        env:
          FEED_URLS: ${{ secrets.FEED_URLS }}
        run: python feed_to_json.py
      - name: Commit if changed
        run: |
          git config user.name "notice-board-bot"
          git config user.email "actions@users.noreply.github.com"
          git add public/notices.json
          git diff --cached --quiet || (git commit -m "Auto-refresh notices" && git push)
